{
  "$schema": "https://schemas.kulpa.io/statements/1.0/schema.json",
  "id": "invalid-missing-fields",
  "name": "Invalid Missing Fields",
  "title": "Invalid Example",
  "textTemplate": "This should fail because 'fields' is missing."
}
